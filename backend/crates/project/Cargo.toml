[package]
name = "relay-project"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
log = "0.4"
notify = "8.1.0"
notify-debouncer-mini = "0.6.0"
serde = { version = "1.0", features = ["derive"] }
